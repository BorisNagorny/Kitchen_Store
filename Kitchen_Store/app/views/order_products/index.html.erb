<h1>Listing order_products</h1>

<table>
  <tr>
    <th>Price</th>
    <th>Created at</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @order_products.each do |order_product| %>
  <tr>
    <td><%= order_product.price %></td>
    <td><%= order_product.created_at %></td>
    <td><%= link_to 'Show', order_product %></td>
    <td><%= link_to 'Edit', edit_order_product_path(order_product) %></td>
    <td><%= link_to 'Destroy', order_product, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Order product', new_order_product_path %>
