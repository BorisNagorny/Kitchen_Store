<%= form_for(@supplier_product) do |f| %>
  <% if @supplier_product.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@supplier_product.errors.count, "error") %> prohibited this supplier_product from being saved:</h2>

      <ul>
      <% @supplier_product.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :cost %><br />
    <%= f.text_field :cost %>
  </div>
  <div class="field">
    <%= f.label :created_at %><br />
    <%= f.datetime_select :created_at %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
